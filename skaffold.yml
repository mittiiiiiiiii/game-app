apiVersion: skaffold/v4beta8
kind: Config
metadata:
  name: koske-game
build:
  artifacts:
    - image: game-app
      docker:
        dockerfile: Dockerfile
    - image: my-api-image
      docker:
        dockerfile: api/Dockerfile
manifests:
  rawYaml:
    - k8s/deployment.yml
    - k8s/ingress.yml
    - k8s/service.yml